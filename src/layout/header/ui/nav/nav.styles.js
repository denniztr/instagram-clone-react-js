import styled from "styled-components";

export const NavContainer = styled.nav`
    display: flex;
    flex-direction: row;
    flex-wrap: nowrap;
    align-items: center;
    gap: 2rem;
`;
