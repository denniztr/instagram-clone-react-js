import * as Styled from './logo.styles.js';

export const Logo = () => {
    return (
        <Styled.LogoContainer>
            <Styled.Logo>
                {/*instapro*/}
                insta
                <span>pro</span>
            </Styled.Logo>
        </Styled.LogoContainer>
    );
};
